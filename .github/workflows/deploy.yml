name: Deploy Cloud Functions

on:
  push:
    branches:
      - main

jobs:

  deploy:

    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Project
        run: |
          gcloud config set project ${{ secrets.GCP_PROJECT_ID }}

      - name: Deploy validateOrder
        run: |
          cd validateOrder

          gcloud functions deploy validateOrder \
            --gen2 \
            --runtime=nodejs20 \
            --region=us-central1 \
            --source=. \
            --entry-point=validateOrder \
            --trigger-http \
            --allow-unauthenticated

      - name: Deploy aiAnalyzer
        run: |
          cd aiAnalyzer

          gcloud functions deploy aiAnalyzer \
            --gen2 \
            --runtime=nodejs20 \
            --region=us-central1 \
            --source=. \
            --entry-point=aiAnalyzer \
            --trigger-http \
            --allow-unauthenticated

      - name: Deploy notifyOrder
        run: |
          cd notifyOrder

          gcloud functions deploy notifyOrder \
            --gen2 \
            --runtime=nodejs20 \
            --region=us-central1 \
            --source=. \
            --entry-point=notifyOrder \
            --trigger-http \
            --allow-unauthenticated

      - name: Deploy Workflow
        run: |
          gcloud workflows deploy order-workflow \
            --location=us-central1 \
            --source=workflow.yaml
